#ifndef FORM_HPP
# define FORM_HPP

# include <exception>
# include <string>
# include <Bureaucrat.hpp>

class Form {
	private:
		const std::string name; // const member variable for form's name
		bool is_signed; //boolean member variable to track if the form is signed
		const int grade_to_sign; 

	public:

};

#endif
